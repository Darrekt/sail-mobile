import 'package:flutter/material.dart';

class PartnerPairingPage extends StatelessWidget {
  const PartnerPairingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 0,
        backgroundColor: Colors.transparent,
      ),
    );
  }
}
